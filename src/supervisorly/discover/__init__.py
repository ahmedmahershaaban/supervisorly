"""Discovery — turning a country/field into reachable directories and target people.

The deterministic discovery ladder (CRIS → sitemap → JSON-LD → OpenAlex/ROR → adapter,
D-028) with **zero LLM calls**. This package currently ships the directory triage that
routes login-walled rosters to the human rung (D-052); the API rungs land here too.
"""
